@page "/page/{StaticPageId:int}"
@rendermode @(new InteractiveServerRenderMode(false))
@using Markdown = Markdig.Markdown

<div class="static-page-content">
@if (_data is not null)
{
    @((MarkupString)Markdown.ToHtml(_data));
}
</div>